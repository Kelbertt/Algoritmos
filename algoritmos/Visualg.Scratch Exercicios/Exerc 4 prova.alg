algoritmo "Contador"
var
   Cont: Inteiro
   X, R: Inteiro
inicio
      Cont  <- 1
      X <- 0
      Enquanto (Cont <= 5) faca
          R <- X * 3
          Escreva (R)
          Cont <- Cont + 1
          X <- X + 1
      FimEnquanto

fimalgoritmo
